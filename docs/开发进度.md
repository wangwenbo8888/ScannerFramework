# ScannerFramework 开发进度文档

> 最后更新: 2026-08-01
> 仓库: https://github.com/wangwenbo8888/ScannerFramework
> 工作目录: E:\workfold\framework
> 最新提交: c6ee8ad

---

## 1. 项目概述

手持式双目多线激光3D扫描仪软件框架，5层架构 + 11产品模块。

**性能指标**: 180fps · 0.01mm精度 · 50-100ms延迟 · Windows/Jetson双平台

---

## 2. 架构完成度

| 层 | 完成度 | 关键组件 |
|---|---|---|
| **HAL** | 60% | CameraControl(Galaxy SDK) · MCUDriver(Win串口) · HardwareMonitor |
| **Data** | 55% | FrameBuffer · PointCloudBuffer · DeviceStateCache · CalibStore |
| **Algorithm** | 85% | 39算子+GBA CPU+CUDA(sm_89)编译通过 |
| **Service** | 60% | StateMachine · ParameterManager · FaultHandler · SessionService |
| **Workflow** | 85% | ScanWorkflow(标记点链) · CalibrationWorkflow(6阶段) · PostProcessWorkflow(7阶段) |
| **UI** | 70% | AppContext装配 · 系统信息面板 · 点云直读 · 帧率分离 |
| **fw_infra** | 60% | EventBus · Scheduler(线程池) · Watchdog(心跳监控) |
| **整体** | **~75%** | |

---

## 3. 已完成的开发阶段

### 阶段1: Data层 ✅
| 文件 | 说明 |
|------|------|
| `FrameBuffer.h/.cpp` | 帧缓冲池(IFrameSink)，DropOldest策略，RingBuffer包装 |
| `PointCloudBuffer.h/.cpp` | 点云双缓冲(IPointCloudSink)，shared_mutex+版本快照 |
| `DeviceStateCache.h/.cpp` | 设备状态缓存(IDeviceStateSink)，原子存储温度/帧率/连接 |
| `CalibStore.h/.cpp` | 标定参数持久化，cv::FileStorage YAML读写 |

### 阶段2: HAL补全 ✅
| 文件 | 说明 |
|------|------|
| `MCUDriver.h/.cpp` | Win串口IMCU实现，协议N10-N15，接收线程解析温度/按键/急停 |
| `HardwareMonitor.h/.cpp` | 周期监控→DeviceStateCache+EventBus，启动即运行 |

### 阶段3: Algorithm算子库 ✅
从 JEAMMWARE260705 迁入 **173个源文件**，CPU+CUDA编译通过。

| 类别 | 数量 | 说明 |
|------|------|------|
| calib16 | 16 | 内参/外参/立体矫正/逆畸变/温度补偿表/光平面映射/姿态优化等 |
| core17 | 17 | 标记点链11 + 激光链4(CUDA) + 视觉2(CUDA) |
| scan6 | 6 | 激光点云融合/法线(CPU+CUDA) + 掩码分离/匹配扫描(CUDA) |
| GBA | +1 | 全局光束平差(需BUILD_GLOBAL_OPTIM=ON+Ceres) |

### 阶段4: Service层 ✅
| 文件 | 说明 |
|------|------|
| `StateMachine.h/.cpp` | 9态状态机，合法转换表+EventBus通知 |
| `ParameterManager.h/.cpp` | variant参数管理，预设相机/扫描默认值 |
| `FaultHandler.h/.cpp` | 订阅FaultOccurred→状态机转Error→红灯事件 |
| `SessionService.h/.cpp` | 启/停/暂停/恢复/checkpoint(断电续扫) |

### 阶段5: Workflow层 ✅
| 文件 | 说明 |
|------|------|
| `WorkflowContext.h/.cpp` | 统一依赖入口，聚合Data×4+Service×3+HAL×2+EventBus |
| `ScanWorkflow.h/.cpp` | 5Stage: Capture→Preprocess(OpenCV阈值)→Marker(Zernike→Ellipse→Match→Reconstruct)→Laser(CUDA)→Fuse(laser_cloud_fuse_cpu) |
| `CalibrationWorkflow.h/.cpp` | 6阶段全接入真实算子: 采集→棋盘检测→IntrinsicCalib→ExtrinsicCalib→StereoRectify→TempTable→写CalibStore |
| `PostProcessWorkflow.h/.cpp` | 七阶段: GBA→重融合→法线→封装→补洞→HC光顺→边界优化 |

### 阶段6: UI重构 ✅
| 文件 | 说明 |
|------|------|
| `AppContext.h/.cpp` | 中央装配: 创建+拥有全部对象，注入WorkflowContext，统一init/shutdown |
| `MainWindow.cpp` | 系统信息面板读DeviceStateCache/PointCloudBuffer，CPU/内存直接Windows API |
| `OSGWidget.cpp` | `loadFromPointCloudBuffer()` 直读点云快照(ADR7.8) |
| `ScannerWindow.cpp` | 帧率统计移到相机回调(反映真实采集帧率)，显示从最新帧读取(不抢FrameBuffer) |

### 阶段7: 管线集成 ✅
- ScannerWindow→ScanWorkflow→StateMachine 事件链
- HardwareMonitor 启动即运行(每秒采集温度/帧率)
- CalibrationWorkflow 标定结果→CalibStore→ScanWorkflow

### 阶段8: CUDA启用 ✅
- RTX 4070 SUPER (sm_89) + CUDA 12.6
- `ENABLE_CUDA=ON` 开启全部CUDA算子编译
- `build_cuda.bat` 构建脚本(解决CMake 3.31 + VS2022 + CUDA兼容问题)

### 阶段9: Infra补全 ✅
| 文件 | 说明 |
|------|------|
| `Watchdog.h/.cpp` | Stage心跳注册/超时检测/回调通知 |
| Scheduler | 完整线程池(submit/waitAll/shutdown) |

---

## 4. 数据流（完整链路）

```
HAL CameraControl (相机回调, 实际帧率)
  ├── ++m_frameCount          → fpsTimer 统计真实采集帧率
  ├── 存 m_latestLeft/Right   → consumerTimer 显示(30fps预览,不抢FrameBuffer)
  └── pushFrame → FrameBuffer → ScanWorkflow 独占消费
                                ↓
                    CaptureStage → PreprocessStage(OpenCV阈值)
                                ↓
                    MarkerStage(CCL→Zernike→Ellipse→Match→Reconstruct)
                                ↓
                    FuseStage(laser_cloud_fuse_cpu → 体素融合)
                                ↓
                    PointCloudBuffer (双缓冲+版本快照)
                                ↓
                    UI OSGWidget (loadFromPointCloudBuffer, 500ms刷新)

HardwareMonitor(1s) → DeviceStateCache → UI 系统信息面板
StateMachine → EventBus → StateChanged
```

---

## 5. 构建配置

### 依赖路径
| 依赖 | 路径 |
|------|------|
| Qt5 | C:\Qt\Qt5.15.2\5.15.2\msvc2019_64 |
| OpenCV 4.13 | F:/opencv4.13/install/x64/vc17/lib |
| OSG 3.6.5 | F:/osg3.6.5/install |
| Eigen 3.4.1 | F:/eigen-3.4.1 |
| CUDA 12.6 | C:\Program Files\NVIDIA GPU Computing Toolkit\CUDA\v12.6 |
| Galaxy SDK | sdk/third_party/galaxy_sdk/ |
| spdlog/nlohmann_json | FetchContent (GitHub mirror: ghfast.top) |

### 构建命令

**CPU-only**:
```bat
cmake -S E:\workfold\framework -B E:\workfold\framework\build -DENABLE_CUDA=OFF
cmake --build E:\workfold\framework\build --config Release --target scan_demo
```

**CUDA (RTX 4070 SUPER)**:
```bat
build_cuda.bat Release
```

### CMake开关
| 开关 | 默认 | 说明 |
|------|------|------|
| ENABLE_CUDA | ON | 启用CUDA算子(sm_89) |
| BUILD_GLOBAL_OPTIM | OFF | 启用GBA(需Ceres) |
| BUILD_TESTS | OFF | 编译单元测试 |

### CUDA构建注意事项
- CMake 3.31 + VS2022 自动注入 `/MP /W4 /arch:SSE2` 到 nvcc，需 `build_cuda.bat` 补丁
- Qt RCC 中文文件名编码问题，需手动 RCC 生成
- Debug 模式需定义 `DEBUG`（非`_DEBUG`）激活 OpenCV `debug_build_guard`

### Git提交历史
```
c6ee8ad feat: 5层架构完整实现 + CUDA算子 + 标定/扫描/后处理工作流
3ba2223 feat: Qt/OSG UI + Galaxy SDK camera + 系统信息面板 + 文档整理
5791cda docs: 新增框架图数据流维度、增强版及P0/P1改进文档
a8ba413 Rename scanner_app to scan_demo, add infra implementations
25092f8 Add .gitignore and remove build/ from tracking
60ce4c3 Initial commit: 5-layer framework skeleton with 25/25 tests passing
```

---

## 6. 待完成工作

### 已知问题
| 问题 | 状态 |
|------|------|
| CMake 3.31 CUDA /MP 注入 | 用 build_cuda.bat 绕过 |
| Qt RCC 中文文件名 | 手动 rcc 生成 |
| 扫描时算子崩溃 | 已加 try-catch 保护 |

### P1 — 后续优化
| 任务 | 说明 |
|------|------|
| ScanWorkflow CUDA激光链 | steger→undistort→epipolar→match→reconstruct 全GPU链 |
| GpuRuntime | ICudaContext + CUDA Stream 管理 |
| 急停同步通道 | publishSync 绕过 EventBus 队列 |
| 标定UI引导 | CalibrationGuideView 标定板姿态偏差显示 |
| 安装包+首配向导 | 模块11 |

---

## 7. 关键设计决策

| 决策 | 理由 |
|------|------|
| Data层唯一所有者 | 上层经窄接口读写；每对象仅一个★写者 |
| UI层全局只读 | 绝无C/U/D；渲染走直读快照(ADR7.8) |
| Algorithm层零数据权限 | 纯函数叶子；调用方负责读写Data |
| HAL经Sink回填 | 依赖倒置：Data定义IFrameSink，HAL推数据不感知Data |
| AppContext中央装配 | 创建并拥有全部对象，注入WorkflowContext，统一生命周期 |
| 显示与处理分离 | 相机回调存最新帧给UI显示，FrameBuffer仅供Workflow处理 |
| 帧率统计在回调中 | m_frameCount在相机回调递增，反映真实采集帧率 |
